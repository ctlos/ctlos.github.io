var wiki = [
{{ range $index, $page := (where .Site.Pages "Section" "wiki") -}}
  {
    id: {{ $index }},
    title: "{{ .Title }}",
    description: "{{ .Params.description }}",
    href: "{{ .URL | relURL }}"
  },
{{ end -}}
];
